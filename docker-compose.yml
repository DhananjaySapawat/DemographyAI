version: '3.9'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - MODEL_URL=http://model:9000

  model:
    build: ./model
    ports:
      - "9000:9000"

